import classTest

# Connect to client
    # On startup broadcast LAN signal
    # Show LAN signals (default names) 
    # If succeed first time, then can send to buffer + client
        # Else try again please

# Get and store user input
    # Read user keystrokes
# Connect to 

HelloObject = classTest.HelloClass()
HelloObject.cprint()
